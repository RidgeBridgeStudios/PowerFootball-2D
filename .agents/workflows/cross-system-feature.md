---
name: cross-system-feature
description: Reusable workflow for safely designing, implementing, and validating gameplay features that span multiple simulation layers and subsystems in PowerFootball-2D.
---

# Workflow: cross-system-feature

Follow the cross-system-feature skill (`.agents/skills/cross-system-feature/SKILL.md`) to safely implement features touching multiple gameplay systems across the 5-layer simulation stack.

## Invocation

- In chat: type `/cross-system-feature` or ask Antigravity to apply the `cross-system-feature` skill.
- The 10 mandatory steps must be followed sequentially.
